spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

jwt:
  secret: test-secret-key-for-jwt-signing-test-secret-key
  expiration-ms: 86400000

logging:
  level:
    root: WARN
    com.example.delivery: DEBUG
